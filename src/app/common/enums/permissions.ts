export enum Permissions {
  Admin = 'admin',
  Contacts = 'contacts',
  Orders = 'orders',
  Products = 'products',
  ViewContacts = 'contacts.view',
  ViewOrders = 'orders.view',
  ViewProducts = 'products.view',
}
